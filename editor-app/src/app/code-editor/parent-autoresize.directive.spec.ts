import { ParentAutoresizeDirective } from './parent-autoresize.directive';

describe('ParentAutoresizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ParentAutoresizeDirective();
    expect(directive).toBeTruthy();
  });
});
